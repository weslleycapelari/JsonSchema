unit TestJsonSchema.RunDrafts;

(*
--------------------------------------------------------------------------------
Executes public JSON Schema draft test suites and asserts validator compliance.
--------------------------------------------------------------------------------
*)


{ [COMMENTED OUT] This unit references the old JsonSchema library which is being replaced.
  The code below is preserved for future reference when reconnecting to the new library. }

{$IFDEF FALSE}
// Old code preserved inside {$IFDEF FALSE} for reference.
// See git history for original implementation.
{$ENDIF}

interface

implementation

end.
